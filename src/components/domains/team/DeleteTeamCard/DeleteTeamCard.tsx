import { VFC } from 'react';
import { Button } from '~/components/parts/commons';

export const DeleteTeamCard: VFC = () => {
  return (
    <Button color="danger" onClick={() => void 0}>
      招待リンクを作成
    </Button>
  );
};
